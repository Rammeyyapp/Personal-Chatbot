raw_full_code folder is created on 18.09.2025

    1. It contain 2 files. 
    2. These 2 files where completely checked. Server.py was connected with LLM. The full integration (client, server, LLM) works successfully

    Work Flow:

        1. Client -> Server -> LLM => LLM -> Server -> Client



Task:

    1. Need to change workflow [ Client -> LLM, LLM -> Client, Client -> Server, Server -> Client ]
    2. For this need client.py and server.py is used.
